{
  "name": "ntb-vendas-print-agent",
  "version": "1.0.0",
  "description": "Agente local de impressao do NTB Vendas -- roda no computador da loja, conectado na impressora, e consome a fila de impressao (print_jobs) do sistema.",
  "private": true,
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js",
    "build:exe": "pkg agent.js --target node18-win-x64 --output ntb-print-agent.exe"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "pkg": "^5.8.1"
  }
}
