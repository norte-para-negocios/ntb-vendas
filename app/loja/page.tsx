import { StoreModule } from '@/components/modules/StoreModule';

export const metadata = {
  title: 'Área do Lojista | Cardápio Digital',
};

export default function LojaPage() {
  return <StoreModule />;
}
