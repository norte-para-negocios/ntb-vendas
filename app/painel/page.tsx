import { AdminModule } from '@/components/modules/AdminModule';

export const metadata = {
  title: 'Painel Admin | Cardápio Digital',
};

export default function PainelPage() {
  return <AdminModule />;
}
